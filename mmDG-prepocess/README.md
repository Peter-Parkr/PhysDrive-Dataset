# Millimeter-Wave Data Preprocessing

This repository contains tools for preprocessing millimeter-wave data. It includes three main steps: running `main.py`, followed by `process_csv.py`, and finally `normalizer.py`.
